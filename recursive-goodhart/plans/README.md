# Plans (recursive-goodhart track)

Lifecycle mirrors the parent `../../plans/README.md`:

- `plans/*.md` — active or current plans, available to execute next.
- `plans/could-do/*.md` — future, optional, or parked ideas; not the current
  workstream.
- `plans/archived/*.md` — completed plans kept for audit trail.

This whole track is parked relative to the parent's era 1 (finish the book
first), so the spine currently lives in `could-do/`:

- `could-do/paper_spine.md` — paper outline + rung formalism + open questions.
  Graduates to the `plans/` root when a human activates the track.
