# Next-mode decision after Iteration 43

Decision: follow the era-1 finishing path.

`research/core-math.md` now gives a stable enough theorem inventory for the
spine refactor to cite without reversing authority. The theorem-shaped core is
worth formalizing later, but Lean is deliberately deferred until era 1 closes
and proposition pointers stop moving.

The remaining open questions are productive only after extra primitives are
declared. Another open-ended research iteration would add audit history before
it changed the manuscript. The artifact that most helps a skeptical reader now
is a clean book/abstract spine built around the consolidated theorem set,
question split, killed claims, and response-modeling contract.

Era-1 finishing order:

1. Run `plans/spine-refactor.md`.
2. Run `plans/prose-pass.md`.
3. Run `plans/polish.md`.

Do not start Lean or new simulations during this finishing path unless a later
plan explicitly changes the era-1 gate.

