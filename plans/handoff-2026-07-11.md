# Handoff — post-litrev state (July 11, 2026)

Pickup doc for a fresh session. Written after Track A's lit-review program fully
closed and after two external reviews of the repo (GPT-5.6 findings, verified
read-only by a second Claude instance). **Until Step 1 below lands, this file
supersedes the stale "Current focus → run Phase 0" block in `next-steps.md`
(~line 51) — Phase 0 is CLOSED; do not re-run discovery.**

Binding norms: `RESEARCH-ITERATIONS.md` (especially Literature-review norms and
the adversarial sub-protocol on `[confident]` claims) + `CLAUDE.md` always-on
norms (claim tags, licensed-claim/overclaim pass, anchor check, attribution
discipline: verify by reading, not memory).

## State snapshot (verified 2026-07-11)

- **Track A lit reviews: ALL CLOSED.** T5 (`looking-for-exchange-rate-results/`),
  T1/T2 (`selection-bound-chapman-robbins/`), T4 (`convex-budget-fenchel/`,
  commit d0a0471), lower-tier L1–L4 (`lower-tier-parallels/`, commit fef0325).
  No escalations. The novelty-refactor capstone is **unblocked**.
- **Paywalled primaries: PULLED.** `sources/` (gitignored — copyrighted PDFs,
  never commit them) now holds every hard-flagged and conditional pull from
  task 08: Rockafellar 1970 *Convex Analysis*, Feltham–Xie 1994, Baker 1992,
  Baker 2002, Holmström–Milgrom 1991, Datar–Kulp–Lambert 2001, Chetty 2009
  (published), Ridgway 1956, Lipsey–Lancaster 1956, Lehmann–Casella *TPE*,
  Gustafson 1996, and BBS 2023 (proxy failure).
- **Track B:** audit closed (single source `citation-audit/SUMMARY.md`); post is
  HUMAN-AUTHORED by decision (July 11) — agent roles limited to outline
  sparring, fact-checking vs SUMMARY.md, overclaim review.
- **External review verdicts (GPT-5.6, verified):** (F1) audit completion was
  ratified plan, not drift, BUT the evidence→publication conversion layer was
  never tasked — SUMMARY.md is audit-shaped, the dictionary/what-transfers
  payoff is not consolidated anywhere; (F2) three overclaim wordings in frozen
  artifacts (details in Step 5); (F3) plan-state contradictions in
  `next-steps.md` and `phase0-handoff.md` — confirmed, live risk; (F4) the
  `empirical_goodhart` workstream has six next steps and no declared consumer.
- Worktree note: the user's own edits (Kerkko→Xylix name/pronoun in
  `next-steps.md` + `could-do/track-b-lesswrong-post.md`, the `sources/`
  gitignore line, settings) may still be uncommitted — fold them into the
  Step 1 commit; they are intentional.

## Order of work

### Step 1 — Plan-state repair (FIRST; cheap; prevents wrong blocking graph)

GPT-5.6 finding 3, fully confirmed. A fresh agent anchoring on the stale text
would be instructed to redo Phase 0 discovery, violating the standing
"consume, don't re-discover" rule.

- `plans/next-steps.md`: rewrite the stale "Current focus → run the unified
  cross-field discovery" block (~lines 51–59) and the Track A pipeline's
  obsolete step "0. (Shared Phase 0) Finish the unified discovery first"
  (~line 117) to reflect: Phase 0 closed June 30; all four Track A reviews
  closed July 11; current focus = Steps 2–3 of this handoff. Point to this file.
- `plans/phase0-handoff.md` §12/§13: tick tasks 07–10 and Phase 1b (done and
  committed — see commits 997e7e3, 44640f8, 6cc7732).
- Include the user's uncommitted name/gitignore edits. Commit to main,
  one line.

### Step 2 — Pinned-number verification pass over `sources/`

Upgrades the `[paywalled-flag-for-user]` / `[verified-secondary]` caps to
`[verified-primary]` now that the PDFs are local. Method: pdftotext (or OCR for
the scanned Baker 1992), read the actual numbered statement, then record.
**No number enters the record unless it was read in the source.**

- Record upgrades as a **dated addendum section** in
  `research/threads/lit_review/cross-field-discovery/prior-art-verification.md`
  (task 08 is a closed audit record — append, don't rewrite its history).
- Targets, in capstone-priority order:
  1. **Rockafellar 1970** — Fenchel duality theorem number (§31; expected
     Thm 31.1 / Cor 31.2.1) + perturbation/value-function conjugacy (§28–30)
     → unlocks T4's pinned citation (currently section-only cap).
  2. **Feltham–Xie 1994** — the congruity/proportionality proposition
     (statement known via Budde, attributed to p. 433) → T5.
  3. **Baker 1992** (scanned — OCR needed) and **Baker 2002** — iff-alignment;
     distortion = 1−cosθ prop numbers → T5.
  4. **Holmström–Milgrom 1991** — multitask / equal-compensation prop numbers
     → related work (NOTE: the book's "HM1991 closest precedent" claim is being
     REMOVED in the capstone, not upgraded — see novelty-refactor R1).
  5. **Datar–Kulp–Lambert 2001** — set-monotonicity proposition → T5.
  6. **Chetty 2009 published version** — check whether eqs. (3)/(7) carry
     "Proposition" labels in the AEJ version; keep w13844 eq numbers otherwise.
  7. **Lehmann–Casella §2.7** — a *numbered* matrix-HCR statement → T2's
     canonical citation (currently Wikipedia-capped skeleton).
  8. **Ridgway 1956** — page-pinned composite-measure quote; **Lipsey–Lancaster
     1956** — page-pinned prose statement (no numbered theorem exists);
     **Gustafson 1996** — only if the Bayesian-robustness third home is cited.
- Propagate each upgrade to its consumers, as **dated post-freeze corrections**
  (never silent edits): the caps/caveats sections of the four litrev READMEs;
  `citation-audit/SUMMARY.md` §3 should-be-cited table; `gap-refs.bib` entry
  annotations. Commit once, one line.

### Step 3 — Novelty-refactor capstone (`plans/novelty-refactor.md`, R1–R5)

**Gate: requires Xylix's explicit green-light** — being handed this plan does
not itself constitute it; confirm in the kickoff message before touching book
content. Reopens the "editing done" milestone. Run with the pinned numbers from
Step 2 in hand. The concrete change list (all verdicts now in):

- **T1/T2**: state as HCR rearranged (Polyanskiy–Wu §29.1 eq. 29.1 / Thm 29.1)
  + χ²-DRO worst case (Namkoong–Duchi Thm 1, √(2ρ/n), with the normalization
  conversion). Survives: selection-channel reading + value-norm packaging.
- **T3** (§5.2): cite Spence / Hardt et al.; Frankel–Kartik 2019 Prop. 2 for
  the family; keep toy framing.
- **T4** (§5.2): demote to "Fact (standard convex analysis)"; cite Rockafellar
  with Step-2 pinned numbers; name Slater explicitly (adversarial-pass note);
  stable label so T3/T5/T6 references survive.
- **T5** (§4.1): cite Feltham–Xie, Baker 1992/2002, Chetty, DKL (pinned);
  REMOVE the "HM1991 closest precedent" claim; restate in cost-normalized
  projection / regression-slope form ("T4-quadratic optimizer + linear harm
  readout"); engage Reynaert–Sallee 2016.
- **T6** (§5.3): static threshold = T4 specialized; recognition-level
  security-games nod; novelty scoped to the termination argument.
- **L1/L2 prose**: anchor non-identifiability in causal inference
  (representation-relativity stays credited as framing + contract role);
  one-line cumulant nod at the covariance refutation.
- **R3**: intro/related-work synthesis-first rewrite; position Reynaert–Sallee
  and Skalse et al. 2022 as the named siblings.
- **R4**: license rows in `claim_audits.md`; `core-math.md` pointers;
  `refs.bib` refresh (merge verified gap-refs entries).
- **R5**: deterministic rebuild; cold read of reframed intro + T5 section.
- Guardrail (both handoffs agree): do NOT overcorrect into "all trivial" —
  the contract, the subset-invariance reading, T6's termination argument, and
  L1's representation-relativity framing stay credited.
- After R5: STOP and report — the post-refactor fresh reading round (Track A
  step 3 in `next-steps.md`) is a separate, human-involved stage.

### Step 4 — Track B conversion layer (parallel; agent-legal)

GPT-5.6's core residual (F1): assemble the missing **publication brief** —
consolidation, not prose. All dictionary rows now exist across the four closed
reviews; they are just not gathered. One document (suggested:
`literature-reference-gap-paper/publication-brief.md`) containing: 4–6
dictionary rows (theorem → native home → citation with verification tag), the
concrete transfer per row, one worked-example pointer, the minimum supporting
audit numbers from SUMMARY.md, and an explicit omit-list (claims the post must
NOT make, from the overclaim passes). **The reader-level thesis sentence is
left blank for Xylix** — that is the authorship decision of July 11; this brief
is squarely inside the sanctioned "outline sparring / fact-check" role. Update
`could-do/track-b-lesswrong-post.md` to name the brief as the single source
(repairing the self-contradicting "single source + three other documents"
pointer, per F1).

### Step 5 — Wording repairs in frozen artifacts (small; before post drafting)

GPT-5.6 finding 2, confirmed. Apply as dated post-freeze corrections:

- `citation-audit/SUMMARY.md` Claim 2 (~line 317): Wang–Huang is NOT
  reinvention evidence — it explicitly imports HM1991/Baker; only its D_i
  index is new-within-the-imported-model (and is a different object from
  1−cosθ, per Pocket 1). Laidlaw's χ² carries the reinvention claim alone.
- `citation-audit/SUMMARY.md` Claim 3 headline (~line 337): "self-reproducing"
  is a dynamics claim on snapshot evidence — rephrase to what the snapshots
  license (e.g., "the gap persists across generations of the canon"), keeping
  the co-located guards.
- `citation-audit/why-the-gap.md` M1 (~lines 56–65): "cannot reach" /
  "structurally misses" → "the tested vocabularies do not cross in either
  direction (two methods)"; the `[confident]` tag covers the disjointness
  fact, not the behavioral generalization to semantic/LLM-assisted search.

### Step 6 — Empirical prototype disposition (decision for Xylix, not agents)

GPT-5.6 finding 4: `empirical_goodhart/` has six internal next steps and no
declared publication consumer (one item gated on Track A's T5 settle, now
settled). Needs a one-line human decision — testbed vs. publication track —
recorded in its README. Agents should not pick; flag and move on.

## Recommended session split

- **Session 1 (no gate):** Steps 1 + 2, commit each. Optionally Step 5 (small,
  same overclaim-repair character) and Step 4 (independent).
- **Session 2 (gated on Xylix's green-light):** Step 3, the capstone.
- Step 6 rides along whenever Xylix decides.

## Concurrency & hygiene

- Check `git status` at session start; parallel workers have been active in
  this repo. Stage by explicit path only (never `git add -A` / `commit -a`);
  scope Edit anchors to your own lines; re-read and retry on edit conflicts.
- Commit directly to main, one-line summaries (repo norm).
- `sources/` stays out of version control; cite page/prop numbers in repo
  docs instead.
- SUMMARY.md, why-the-gap.md, and the task-08 verification file are closed
  records: corrections are dated addenda, never silent rewrites.
